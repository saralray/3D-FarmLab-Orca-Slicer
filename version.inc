# Included by CMakeLists, edited by the build script
# (the version numbers are generated by the build script from the git current label)

# Displayed application name. SLIC3R_APP_KEY is kept as "OrcaSlicer" because it
# drives the user data directory (~/.config/OrcaSlicer) and translation catalogs;
# changing it would orphan existing settings/presets.
set(SLIC3R_APP_NAME "3D-FarmLab-Orca-Slicer")
set(SLIC3R_APP_KEY "OrcaSlicer")

if(NOT DEFINED BBL_INTERNAL_TESTING)
set(BBL_INTERNAL_TESTING "0")
endif()
set(SoftFever_VERSION "2.5.0-dev")
string(REGEX MATCH "^([0-9]+)\\.([0-9]+)\\.([0-9]+)"
       SoftFever_VERSION_MATCH ${SoftFever_VERSION})
set(ORCA_VERSION_MAJOR ${CMAKE_MATCH_1})
set(ORCA_VERSION_MINOR ${CMAKE_MATCH_2})
set(ORCA_VERSION_PATCH ${CMAKE_MATCH_3})

set(SLIC3R_VERSION "02.06.00.51")

if (NOT DEFINED ORCA_UPDATER_SIG_KEY)
    set(ORCA_UPDATER_SIG_KEY "" CACHE STRING "Base64url encoded updater signature key")
endif()
